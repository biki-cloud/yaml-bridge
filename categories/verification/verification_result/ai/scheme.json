{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "verification/verification_result.schema.json",
  "title": "動作確認結果",
  "description": "動作確認の結果を記録するドキュメント",
  "type": "object",
  "required": ["meta", "test_results", "summary", "references"],
  "properties": {
    "meta": {
      "allOf": [
        { "$ref": "../../../../common/scheme.json#/definitions/meta_base" },
        {
          "properties": {
            "category": { "const": "verification" },
            "doc_type": { "const": "verification_result" }
          }
        }
      ]
    },
    "overview": { "$ref": "../../../../common/scheme.json#/definitions/overview_background_goal" },
    "target": {
      "type": "object",
      "properties": {
        "feature": { "type": "string" },
        "pr_url": { "type": "string" },
        "environment": { "type": "string" }
      },
      "x-ai-guid": "対象。feature, pr_url, environment。"
    },
    "test_results": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "status"],
        "properties": {
          "id": { "type": "string" },
          "title": { "type": "string" },
          "status": { "type": "string", "enum": ["pass", "fail", "blocked", "skipped"] },
          "actual_result": { "type": "string" },
          "notes": { "type": "string" }
        }
      },
      "x-ai-guid": "テスト結果一覧。id, status（pass/fail/blocked/skipped）必須。title, actual_result, notes。"
    },
    "summary": {
      "type": "object",
      "required": ["conclusion"],
      "properties": {
        "executed_at": { "type": "string" },
        "executed_by": { "type": "string" },
        "total": { "type": "integer" },
        "passed": { "type": "integer" },
        "failed": { "type": "integer" },
        "blocked": { "type": "integer" },
        "skipped": { "type": "integer" },
        "pass_rate": { "type": "number" },
        "conclusion": { "type": "string" },
        "issues_found": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "id": { "type": "string" },
              "description": { "type": "string" },
              "severity": { "type": "string", "enum": ["blocker", "critical", "major", "minor"] },
              "status": { "type": "string", "enum": ["open", "fixed", "wont_fix"] }
            }
          }
        }
      },
      "x-ai-guid": "サマリー。conclusion 必須。executed_at, executed_by, total, passed, failed, blocked, skipped, pass_rate, issues_found。"
    },
    "references": {
      "$ref": "../../../../common/scheme.json#/definitions/references",
      "x-ai-guid": "共通は common/scheme.json#/definitions/references。1件以上の { title, url }。"
    },
    "ai_context": {
      "$ref": "../../../../common/scheme.json#/definitions/ai_context",
      "x-ai-guid": "任意。共通は common/scheme.json#/definitions/ai_context。"
    }
  }
}
